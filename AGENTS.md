# Agent Instructions

## Roadmap

- [ ] **Add a `useMemo`/`useCallback` optimisation pass**: audit components for values that can be memoised with `useMemo` and ensure every computed value / handler is as stable as the conventions already enforce for handlers.
- [ ] **Expand unit-test coverage**: the Vitest + React Testing Library + jsdom harness is in place (`vitest.config.ts`, `test/vitest/setup.ts`, `test:coverage` in the pre-push hook and CI) with suites under `src/**/test.unit.ts*`; remaining work is broader component coverage (`handle*` handlers, `classNames` conditionals) and ratcheting the coverage thresholds.
- [ ] **Integration tests**: recommended tool is **Vitest** (single runner shared with unit tests) + **`next-test-api-route-handler`** for `/api/*` route handlers and `lib/db` queries against the Docker Postgres. Run them against a test database to avoid clobbering dev data. (Alternative if a separate HTTP layer is preferred: `supertest` against `bun run start:prod`.)
- [ ] **E2E tests**: add Playwright (already available via the Playwright MCP server). Cover the critical journey: sign-up → identify (mocked PlantNet response) → add plant → care due → mark done.
- [ ] **Refresh `care_reference` from a scheduled job instead of a migration**: the table is a point-in-time snapshot (134 rows from the 2026-09-16 PlantSolve export) and only changes via a new migration + deploy, so refreshing the data means shipping code. Investigate a Vercel Cron that pulls the latest export, maps it into the same value+unit shape and upserts on the existing `(match_type, match_key)` unique index — the constraint that makes the seed idempotent is the same one an upsert needs, and each row must keep its `source_url` (the CC BY 4.0 backlink). Settle these first: whether PlantSolve publishes a versioned feed or only a manual export, how to record which snapshot a row came from, and how to validate before writing so a bad row fails the job rather than `resolveCare`'s loader throwing at identification time.
- [ ] **Make the install guide browser-aware, not just OS-aware**: `getInstallPlatform` (`src/services/install/index.ts`) branches on `os.is(OSName.IOS)` and `engine.is(EngineName.BLINK)` only, so every iOS browser lands in `InstallPlatform.Ios` — and `INSTALL_GUIDE_CONTENT[Ios]` opens with "Tap the Share button in **Safari's** toolbar", a Safari-only affordance, shown to someone using Chrome or Firefox on the same phone. Measured with the UAs that matter: iOS Safari → `browser=Mobile Safari`; iOS Chrome → `Mobile Chrome`; iOS Firefox → `Mobile Firefox` — three browsers, one bucket, one wrong set of steps. `BrowserName` is already reachable from the `ua-parser-js/enums` module this file imports for `OSName`/`EngineName` (`SAFARI`/`SAFARI_MOBILE`, `CHROME`/`CHROME_MOBILE`, `FIREFOX`/`FIREFOX_MOBILE`), so no new dependency is needed. Two constraints to settle before writing copy. (1) **Key on browser *and* OS, never browser alone** — `Mobile Chrome` is what iOS Chrome and Android Chrome both report, and their steps differ (iOS has no three-dot menu; Android does), so a browser-only key silently reintroduces the same bug. (2) **Chrome/Firefox on iOS is a dead end, not a copy variation** — a web app added to the Home Screen from a non-Safari iOS browser is a WKWebView shortcut that cannot receive Web Push, so those users must be told to open the page in Safari *first* and install from there, not handed Chrome's own add-to-home-screen steps. That is the user-facing consequence of getting it wrong: they follow the instructions, the install appears to work, and reminders never arrive. Touch `InstallPlatform` (`src/services/install/types.ts`), `getInstallPlatform`, `INSTALL_GUIDE_CONTENT` (`src/containers/SettingsScreen/InstallGuide/constants.ts`), and the `AuthScreen` install path that shares the platform signal.
- [ ] **Password reset**: there is no way to recover an account today — `emailAndPassword` is configured in `src/lib/auth/index.ts` with no `sendResetPassword` callback, and the sign-in screen has no "forgot password" affordance at all. Token storage already exists: Better Auth generated the `verification` table (with its `identifier` index), so the flow itself needs no migration. The work is (a) wiring `emailAndPassword.sendResetPassword` to a `forgetPassword`/`resetPassword` client flow plus a reset screen, and (b) **email delivery, which is the real cost** — the app installs no mail transport whatsoever (no nodemailer, Resend, Postmark, SES or SMTP), so this adds a provider, a verified sending domain and a new env var, and the reset message becomes the first transactional email the app has ever sent. Settle three things before building. (1) **Whether email verification has to ship alongside it** — `requireEmailVerification` is off today, and allowing a password reset for an address nobody has proven they own is the classic account-takeover vector; if reset ships, verification arguably has to ship with it, which is a second flow and a second email. (2) **What a Google-OAuth-only account resets** — those users have no password to change, so the flow must either hand them back to Google or offer "set a password"; a reset link that lands on a passwordless account is a dead end. (3) **Token TTL and single-use semantics**, and that the response stays indistinguishable for a known vs unknown email (Better Auth's `forgetPassword` is built to answer success either way — keep that property). Touch `src/lib/auth/index.ts`, `src/lib/auth/auth-client.ts`, `src/containers/AuthScreen/` and the `/login` route, plus `.env.example`.
- [ ] **Let fertilise and repot be "never" on the add path, and make never anchor its countdown at today**: two halves of the same gap. (1) **Defaults.** The add form forces both kinds into a schedule — `AddPlantForm` seeds `care` from `result.defaultCare` (the PlantSolve-derived values) and `resolveLastCare` turns a blank last-care date into **today** for all three kinds alike, so an untouched add form always yields a fertilise and repot schedule anchored at today, even though the frequency selects already offer `never` (value `0`) and the edit pane uses it. **Settle which change is wanted before touching code**, because the two readings hit different files and one is a product decision rather than a bug fix: (a) default the *frequency* to `never` by zeroing `fertilizeEveryDays`/`repotEveryMonths` in `AddPlantForm` instead of taking the PlantSolve values — note this discards the `care_reference` defaults for those two kinds on the add path; or (b) make a blank last-care *date* for those kinds mean never, i.e. stop `resolveLastCare` defaulting them to `startOfToday`. Water is unaffected either way (it has no `never` option). (2) **Display and anchor.** With a zero interval `nextDue` returns `undefined`, so the plant-detail stat shows `'—'` (`NO_DUE_DATE_TEXT` in `CareStatValue/constants.ts`), the task never enters `dueTasks` and no reminder fires — all correct, and worth keeping. Two changes are wanted: say `never` instead of `'—'`, matching what `CareScheduleNotice` already prints for the same condition, and record **today** as that kind's `lastCare` when never is chosen so that switching back to a real interval counts from today. That second gap is concrete: `EditSchedule` writes only `nickname` and `care` — never `lastCare` — so a plant moved interval → never → interval counts from the original, potentially months-stale date. Keep the never-kind out of `dueTasks` regardless; showing it as due today would nag forever.
- [ ] **"Notifications are enabled." should reflect a real subscription, not just permission**: `RemindersCard` renders `renderEnabled` whenever `perm === 'granted'`, so the card claims success when `pushManager.getSubscription()` returns null — and because the Enable button then disappears, the only path that created a subscription became unreachable, which is how an iOS device that granted permission before the app was installed to the Home Screen ended up permanently unsubscribed with no way back from the UI. The subscription is now re-established on every load by `ensurePushSubscription` and the test control reports the truth (see the Notes entry), so what remains is this honesty gap: surface subscription existence from `ensurePushSubscription` through `useNotifications` and gate `renderEnabled` on it. The same card's hint still says checks run "in the background (installed app on Android/Chrome)", which omits iOS and should be corrected while you are in there.
- [ ] **A subscription created under a different VAPID key is never recovered or pruned**: found by driving the real UI in a browser (2026-09-22). If the VAPID keypair is ever regenerated, every existing subscription becomes permanently unusable — FCM answers **403 `the VAPID credentials in the authorization header do not correspond to the credentials used to create the subscriptions`** — and two gaps keep it that way. (1) **Only 410 is pruned.** `sendPushToSubscriptions` deletes a row only on `statusCode === 410`, so a 403 is logged as a generic failure and the dead row is retried on every send forever. (2) **`ensurePushSubscription` re-affirms a stale subscription without checking its key.** It calls `pushManager.getSubscription()` and re-POSTs whatever it finds, so a subscription made with the old key is kept rather than replaced — the row looks healthy while every send 403s, which is the same silent-death shape as the original bug. The fix is to compare the subscription's own `options.applicationServerKey` against the current `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `unsubscribe()` and re-subscribe when they differ, and to treat a 403 as prunable. Low priority only because the keys have never been rotated; the failure is silent and total if they ever are.

## Done

- [X] **Migrate Vite PWA → Next.js 16 App Router** (in place). Routes, server actions, auth (Better Auth), Postgres (Drizzle), PWA hardening, Dockerized local dev.
- [X] **Auth** — Better Auth with Google OAuth + email/password; per-user scoping on every query.
- [X] **Dockerized local dev** — `docker compose up -d db` + `bun run start`, or `docker compose up -d --build` for both containers.
- [X] **Handlers & style conventions enforced by lint** — all event handlers are `handle*` arrow functions wrapped in `useCallback` (no inline handlers), all class names come from `*.module.css` via `styles.x`, no single-line object literals, blank line before every `return`, curly braces on all blocks. Enforced by custom rules in `eslint-rules/` (`no-literal-classname`, `no-inline-object-literal`, `no-inline-handlers`) + `@stylistic/padding-line-between-statements` + `curly`.
- [X] **React conventions** — every component is `React.FunctionComponent<Props>` with `Props extends React.ComponentProps<'element'>` (or `Omit`), destructures and spreads `{...props}` on its root element. One component per `index.tsx` (own folder each); style/style constants live in sibling `constants.ts` files. Enums over magic strings (`CareKind.Water` not `'water'`). `lodash-es` over hand-rolled utils. `classNames` for every multi-class/conditional class via `const X = classNames(styles.root, { [styles.x]: cond })` at the top of the component above the `useState` calls (never inline in JSX, object-map form for conditionals, single class stays inline). All icons are `lucide-react` components (`CARE_META` holds `icon: LucideIcon`, not emoji strings). These are codified globally in the `frontend-code-conventions` opencode skill (`~/.config/opencode/skills/frontend-code-conventions`). See the Tools table below.
- [X] **Dependency updates** — `renovate.json` (`config:recommended`) keeps dependencies current.
- [X] **CI with GitHub Actions** — `.github/workflows/ci.yml` runs `build:rules` → `lint` → `test:coverage` → `build` → `test:size` on pushes to `master` and on pull requests.
- [X] **Accessibility pass** — layout-owned `<main>` + skip link, accessible names on icon-only controls, `:focus-visible` styling, reduced-motion support, and browser zoom across every screen. See the Phase 8 entry in `.omo/plans/plant-app-normalization.md`.
- [X] **Conventional commits enforced** — Husky hooks (`pre-commit` → `lint:staged`, `commit-msg` → commitlint, `pre-push` → lint + `test:coverage` + build + `test:size`) with `@commitlint/config-conventional`.
- [X] **Design-system blocks** — `Button` (union button/anchor; `Default`/`Primary`/`Secondary`/`Danger`/`Soft`/`Outline`/`Unstyled`), `Select`, `AlertDialog`, `DatePicker` and `Popover`, all in `src/design-system/`. Every block wraps a Base UI primitive except `Button`'s anchor branch, which stays `next/link`: Base UI's Button enforces button semantics and its docs say it should not be used for links.
- [X] **React #418 hydration warning — closed** (pinned 2026-09-12, closed 2026-09-17). Two causes, both gone. (1) The reproducible hit (Wave 1: `/settings`, both visits) came from client-only state read during render in `useState` initialisers — `getPlantNetKey()` (`localStorage`, server `''`) and `Notification.permission` (server `'denied'`, since Node's `navigator` has no `serviceWorker`) — removed in `b7d7222`, which dropped the user-supplied PlantNet key for the shared one and replaced the permission read with the hydration-safe `useNotifications` hook (`undefined`/`'default'` initial state, real values applied in an effect). (2) The `<div>`-inside-`<button>` nesting in `TaskRow` (the original suspect) is gone: the unstyled `Button` holds only `<span>`s and `PlantPhoto` always renders `<img>`. No browser API is read during render now, so nothing else diverges deterministically. Residual, deliberately not fixed: `useClock`'s `Date.now()`. Every clock-derived string is day-granular (`formatDue`, `formatDaysAgo`) and flips only when hydration straddles local midnight — an SSR → `hydrateRoot` probe rendered `In 7 days` server-side vs `In 6 days` client-side for `23:59:59.5 → 00:00:00.5`, because `lastCare` is local midnight and intervals are whole-day multiples. React regenerates the subtree client-side; eliminating it would mean seeding `useClock`'s initial state with a server-computed `now`.
- [X] **Web Push reminders** — `web-push` plus a daily Vercel Cron, so care reminders arrive with the app closed. `push_subscriptions` (migration `0003_added_push_subscriptions`) holds one row per device; `POST /api/push/subscribe` registers it behind `requireUser()`; `GET /api/cron/care-check` is the sender, gated on `CRON_SECRET`, and prunes endpoints the push service answers with `410`. The service worker's `push` listener displays the notification immediately, because Safari revokes the permission if it does not. Needs `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` and `CRON_SECRET` in the deployment; the cron is daily because Vercel Hobby rejects anything more frequent. Hardened 2026-09-22 — day-granular de-duplication, sends recorded only when they succeed, and the subscription re-established on every app load. See the Notes entries for the two defects that made the cron silently skip days and for why the periodic-sync path was removed.

## Notes

- ESLint is type-aware and slow; `bun run lint` (not `--fix` in a loop) is the reliable check.
- `next build` does **not** run ESLint; lint is a separate `bun run lint`.
- Component styles are Tailwind CSS modules: every `styles.module.css` must start with `@reference "<relative>/app/globals.css";` or its `@apply` will not resolve. `src/app/globals.css` deliberately omits Tailwind preflight (the app ships its own reset).
- The proxy matcher in `src/proxy.ts` must stay a plain string constant (Turbopack requirement); `unicorn/prefer-string-raw` is disabled for that file in `eslint.config.mjs`.
- `src/lib/db/auth-schema.ts` is generated by Better Auth CLI and lint-ignored; don't edit by hand.
- Server actions need `bodySizeLimit` (5 MB) for photo uploads — configured in `next.config.ts`.
- The plant photo Route Handler lives at `src/app/(app)/plants/[id]/photo/route.ts`, not under `/api`. That path is the stored `Plant.photo` URL produced by `queries.rowToPlant`; route groups scope layouts, not Route Handlers.
- **No `next/image`** (deliberate deviation): the `requireUser()`-gated `bytea` photo route can't be read by the Image Optimizer because it forwards no cookies, and the two capture-preview `<img>` sites use unsupported `blob:` URLs. Keep `PlantPhoto` and the previews as plain `<img>`.
- **Font** is self-hosted Manrope via `next/font/local` in `src/app/layout.tsx` (loader inlined there, not `next/font/google`): `--font-manrope` is wired to `--font-sans` in the `@theme static` block. Never add a runtime Google font request.
- **Boundary model:** `(app)/error.tsx` does not catch `(app)/layout.tsx` errors (they reach the root `error.tsx`); `src/app/global-error.tsx` covers root-layout failures; `(app)/loading.tsx` covers the page's data fetch but not the auth gate (`requireUser()` reads `headers()`, which blocks navigation before the loading boundary).
- **Auth: Better Auth stays; Keycloak is deliberately not adopted** (decided 2026-09-17). This is a **deliberate deviation from the global `frontend-code-conventions` skill**, which names Keycloak as the auth layer for every app. Per-app auth is a decision, not a default: the apps after Sprout each get their own auth and do not need SSO between them. That removes Keycloak's whole value proposition (centralization) without removing the per-app integration work — `requireUser()`, the proxy check and the session wiring survive either way, because an app stays a thin OIDC client — so Keycloak would add a JVM, a second datastore, ~1.25 GB RAM, a second upgrade cadence and a shared point of failure across every app, to avoid only credential handling. Better Auth is the lighter fit and deploys wherever the app does, including serverless. **Accepted consequences:** no SSO between apps, no admin console, no audit log, no built-in brute-force lockout, and MFA only via Better Auth's `two-factor` plugin plus our own enrolment UI. The discipline that replaces a shared IdP is reusing **one auth pattern** across apps (one `requireUser()` contract, one session config, one password policy). **Revisit when either:** (a) a concrete MFA / LDAP / compliance driver appears, or (b) work starts on the **next app** — at which point evaluate a **separately-hosted shared Keycloak** (one instance, a realm per app, **no SSO required**, ~$5–10/mo on a small VPS) *before* app #2 or #3 ships, since that is the last cheap moment. Not on the "no SSO" premise alone: without SSO a shared instance still pays off by removing per-app credential handling, password reset, email verification, MFA, brute-force lockout, audit events and the admin console, plus one Google IdP config instead of one per app — though it does **not** shrink per-app code, because each app keeps an OIDC redirect, token validation and the cookie/session layer a server-rendered app needs, and it creates one shared failure domain (Keycloak down ⇒ every app down). **Switch cost scales with app count *and* accumulated user data**, so the cheapest moment to reverse this was Sprout itself (declined on purpose) and the next cheapest is before the second app ships — not after the fifth.
- **JWT / bearer-token auth: investigated and NOT adopted** (2026-09-17). Sprout stays on DB-backed cookie sessions; three findings here are worth not re-deriving. (1) **`bearer()` does not validate JWTs** — it accepts Better Auth's *opaque session token* and rewrites it into the `session_token` cookie, so a `jwt()`-issued token fails its HMAC check; the two plugins are not interoperable in the direction you would expect. (2) **`jwt()` is not a session replacement** — its `/token` endpoint requires an existing session, and it needs durable key storage (the `jwks` table, or a custom `adapter`). (3) **Cookie-only ("stateless") sessions are impossible while a database adapter is configured**: `hasServerSessionStore = !!database || !!secondaryStorage` has no override, `session.storeSessionInDatabase` only applies when `secondaryStorage` is set, `cookieCache` is only a cache over a DB-backed row (a read still happens on miss/expiry), `getAccessToken` forces a cache bypass when stateful, and dropping the database is off the table anyway because `plants.userId` FKs `user.id` with `onDelete: 'cascade'`. **Consequence:** the only way to move sessions out of Postgres is `secondaryStorage` (Redis) — a new container, to save one indexed lookup on a table we already have. **JWKS has no consumer here** (no second client), so `jwt()` would add a table plus rotation config to enable verification that nothing performs. Verified against Better Auth v1.7.5 source; the option names and the "stateless only without a DB" semantics also hold in the pinned 1.6.29.
- **Server timezone: deliberately no `TZ` env var** (2026-09-19). Vercel **reserves** `TZ` (with `NOW_REGION`, `AWS_*`, `LAMBDA_*`), so it cannot be set from the dashboard or the CLI — but nothing needs it, and the care engine must not be "fixed" by adding one. The server only ever does arithmetic on **absolute instants**: `dueTasks`/`nextDue`/`daysUntil`/`formatDue`/`formatDaysAgo` compare and subtract `Date.now()` against stored `lastCare + interval` timestamps, so they are timezone-independent and correct on a UTC host. The timezone-*dependent* helpers are `toDateValue`, `startOfToday`, `resolveLastCare` and `today(getLocalTimeZone())`, and `toDateValue` provably differs per process zone: `1789768800000` (19 Sep, local midnight) is `2026-09-19` under `TZ=Europe/Amsterdam` but `2026-09-18` under `TZ=UTC`, because `lastCare` is stored at *local midnight* — 22:00 UTC the previous day. That never reaches server-rendered HTML: every `toDateValue` call site (`CareStatValue`, `CareLogRow`) sits inside `BasePopover.Portal` with no `keepMounted`, so the popup is absent from the initial HTML and renders client-side in the browser's zone; the writes (`startOfToday()`, `parseDate(...).toDate(getLocalTimeZone())`) run in click handlers in the browser; and the `max` dates in `LastCareFields`/`CareLogRow` are consumed only inside client-navigated forms and popups. `getLocalTimeZone()` is therefore deliberate and correct here — pinning a fixed app timezone would be a behaviour change, not a fix. **Accepted residual:** a date picker's `max` can be a day stale between 00:00–02:00 Europe/Amsterdam, only in the client-navigated add/edit forms, and it is recomputed client-side on interaction. Related build fact: `next.config.ts` gates `output: 'standalone'` behind `process.env.VERCEL`, because Next 16.3.x + `standalone` breaks Vercel builds (`ENOENT .next/next-server.js.nft.json`); `.github/workflows/deploy.yml` sets `VERCEL=1` explicitly on the build step because Vercel exposes `VERCEL` only when the project's "Enable access to System Environment Variables" toggle is on, so the guard must not depend on that toggle.
- **PWA cache invalidation is keyed to the deployed commit, and lives in code — not in `deploy.yml`** (2026-09-19). The service worker's cache name is `sprout-<BUILD_ID>`, derived from the `?v=` query string on the registration URL (`public/sw.js` reads `location.search`) rather than a hand-bumped literal. `SiteCore` registers ``/sw.js?v=${BUILD_ID}``, where `BUILD_ID` is `NEXT_PUBLIC_BUILD_ID` inlined at build through `next.config.ts`'s `env` block (`NEXT_PUBLIC_BUILD_ID ?? VERCEL_GIT_COMMIT_SHA ?? 'dev'`); `.github/workflows/deploy.yml` sets it to the deployed commit SHA. This works because the spec's update check skips re-installation only when the script URL **and** the body bytes are both unchanged, so a changed query string alone forces a new worker — whose existing `activate` handler deletes every cache whose name is not current. Verified in a browser: seeding `sprout-OLDSTALE` + `sprout-verify123` and then registering `?v=verify456` left exactly `['sprout-verify456']`. **Do not** "simplify" the registration back to a bare `/sw.js`, and **do not** fingerprint the worker's filename (the byte-comparison then has no stable URL to compare against) — either mistake freezes the shell cache across deployments, which is exactly how a change to `manifest.ts` or an icon would never reach an installed PWA. A `deploy.yml` step cannot do this on its own: Cache Storage lives on the user's device, so only the worker can purge it.
- **`vercel pull` writes `[SENSITIVE]` placeholders, so the prebuilt deploy supplies its own build-time env** (2026-09-19). `vercel pull` downloads the project's env into `.vercel/.env.production.local`, but Vercel refuses to hand back any variable marked **Sensitive** — it writes the literal string `[SENSITIVE]` instead. `next build` then evaluates `betterAuth()`, which validates `BETTER_AUTH_URL` at module scope and throws `Invalid base URL: [SENSITIVE]` while prerendering `/_global-error`, failing the whole build. `process.env` wins over that file, so `.github/workflows/deploy.yml` sets real `DATABASE_URL` / `BETTER_AUTH_SECRET` / `BETTER_AUTH_URL` on the `vercel build` step; runtime still reads the project's own env on Vercel, so a placeholder secret there is harmless. **Do not delete those three vars as redundant** — they are what keeps the build alive, and any future module-scope `process.env` read that validates its input will need the same treatment. Note also that `vercel build` *requires* `pull` first (it otherwise fails with `No Project Settings found locally`), so the step cannot simply be dropped, and `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` alone are not enough for it. The same placeholder reaches the *browser* through prerendered pages: `/login` and `/signup` were `○ (Static)` in the build's route table and read `process.env.GOOGLE_CLIENT_ID` in a server component, so the generated HTML shipped a Google Sign-In button whose client id was the literal `[SENSITIVE]` — a 403 from `accounts.google.com` on every visit, instead of the intended "Google sign-in is unavailable right now" notice. Both pages now declare `export const dynamic = 'force-dynamic'` so the id is read at request time; any future prerendered page that reads an env var marked Sensitive needs the same treatment, and the `export const dynamic` line must not be removed as a "needless optimisation".
- **ADRs are retired — do not cite or create them.** The Nygard-format records that used to live in `README.md` (ADR-0001 … ADR-0011) were deleted in `c3fc1b7` when the README was rewritten to its current 90 lines, and they are not being reinstated. Any surviving `ADR-00xx` reference in gitignored `.omo/` scratch is historical and points at nothing. Deliberate decisions — **including deviations from the global skills** — are recorded in this Notes section instead (see the Auth note above, plus the `no next/image` and font notes).
- **iOS ignores `box-sizing` on the native date control, so the pickers are sized with `-webkit-fill-available`** (2026-09-20). On iPhone the three `LastCareFields` date inputs overflowed the card and ran 11px past the viewport edge while every desktop browser looked correct. Measured on-device: `input=388 parent=361 vw=393 box=content-box` — `width: 100%` resolved to the parent's 361px and the padding (12+12) plus the border (1.5×2) were added **on top**, 27px in total. This is not a cascade problem and not a missing reset: `globals.css` ships `* { box-sizing: border-box }`, that rule is present in the deployed chunk, and an **inline, non-`!important` `box-sizing: border-box` is ignored too**; `width: 50px` does shrink the field to 50px, so it is not an intrinsic minimum either. The native control simply does not honour `box-sizing` on iOS. `DatePicker/styles.module.css` therefore sets `input { width: -webkit-fill-available }`, which measures 361 on iOS and resolves to the same available width as `w-full` elsewhere — engines that do not recognise the value drop it and fall back to `w-full`. **Do not** replace it with `box-sizing: border-box` or delete it as redundant: both were measured on the device and leave the 27px overflow in place. `appearance: none` also reaches 361 but is rejected — it strips the native control and runs into WebKit bug 198959, where an empty date input collapses in height unless `::-webkit-date-and-time-value` is given a height, so it trades a width overflow for a height bug. Two adjacent facts that are **not** defects and must not be "fixed": iOS renders **no calendar indicator** on date inputs (a Chromium-only affordance), and it renders an **empty date input as completely blank** — there is no date-input placeholder to style, `::placeholder` cannot target it, and the `content: attr(placeholder)` workaround needs JS to detect the empty state. Empty-state guidance belongs in the existing `hint` prop (`AddPlantForm` already passes `LAST_CARE_HINT`), which reads the same on every platform.
- **Do not add `capture` to the photo-upload file input** (2026-09-20). `CaptureStage`'s hidden `<input type="file" accept="image/*">` must stay free of `capture`; the attribute reads like a helpful hint, but on iOS it opens the camera app directly and **suppresses the Photo Library / Take Photo / Browse sheet entirely**, so the control labelled "Upload a photo of your plant" cannot reach the library at all. The screen already owns a dedicated live-camera flow (`handleStartCamera`/`handleCapture`), so the input is library-first by design — `capture` would both duplicate the camera and remove the only path to an existing photo. Omitting it fixes the same camera-forcing behaviour on Android.
- **Care defaults are per-genus, sourced from PlantSolve (CC BY 4.0), and live in Postgres — the bundled table is gone** (2026-09-20). `care_reference` holds 134 rows — 76 genera, 37 families and 21 aliases — reduced from a 2026-09-16 snapshot of the [PlantSolve Open Plant Care Dataset](https://www.plantsolve.com); `src/helpers/care/constants.ts` keeps only the hand-authored `FALLBACK_CARE`. The seed is a custom Drizzle migration (`drizzle/0002_seeded_care_reference_table.sql`) applied by the `migrate` job that gates deploy, not a script. PlantSolve is the only source found that carries **all three** cadences — watering, fertilising **and** repotting — under a licence that permits commercial use. **The table lives in Postgres now, and the shape of the access is what keeps that honest.** One 134-row select loads the whole reference per identification and resolution stays an in-memory property access — a per-identification *point* lookup would still be the wrong shape, but there is none. The unique index on (`match_type`, `match_key`) makes the seed idempotent (`ON CONFLICT DO NOTHING`) and curation an `UPDATE` rather than a deploy; Each cadence is stored as a value+unit pair (`water_interval`/`water_unit`, `fertilize_interval`/`fertilize_unit`, `repot_interval`/`repot_unit`) with the unit typed by `CareUnit` (`D`/`M`/`Y`), and the CHECK constraints convert each pair to canonical days inline (`D`=1, `M`=30, `Y`=365) so the horticultural bounds survive (water 2–28, fertilise 14–90 days, repot 6–36 months), replacing the unit tests that asserted those ranges over the generated file — each check requires the pair to be **both NULL or both set**, because `NULL in ('D','M','Y')` evaluates to NULL and a CHECK treats NULL as passing, so a bare unit-membership test lets an interval be stored with no unit (repot was the live case: `repot_interval=2` with a NULL unit lands on 730 days, inside the 180–1095 window, so the range alone did not catch it); the loader (`src/services/server/care-reference/index.ts`) converts the pairs back to the app's `CareSchedule` (`waterEveryDays`, `fertilizeEveryDays`, `repotEveryMonths`) and throws when a genus/family row is missing a value or a unit, so no unit ever reaches the app model. Per-user care rows are a different thing and stay in `plants.care` (jsonb) exactly as before — `care_reference` only *seeds* that column, and it must resolve before any plant row exists. Rejected alternatives, recorded so they are not re-evaluated: **Perenual** is the only source publishing a numeric watering interval (`watering_general_benchmark`, a `"5-7"` string plus `"days"`) but its ToS forbids commercial use *and* derivative works, and it has no fertilise or repot field at all; **FloraDB** has an explicit `watering_frequency_days` for 270 houseplants but its free sample is CC-BY-NC and the commercial set is $49; **OpenPlantBook** returns care as free-text prose with no interval, caps at 200 requests/day, and is a single-maintainer project with no SLA; **Trefle**'s growth fields are Ellenberg wild-habitat classes (1–9 and 1–12, not care preferences) covering 1.4% of species; **Kindwise** offers only an ordinal `watering` (1–3, dry/medium/wet) at 9.5% coverage; **Pl@ntNet** carries zero care fields — confirmed by scanning its entire published `swagger.json`, and its own FAQ points integrators to GBIF for more species data. Two consequences are load-bearing. (1) **`waterEveryDays` is derived, not measured** — PlantSolve publishes no day-count, only `drynessThreshold` (25/50/100) and `growthRate`, so the base is 4/7/14 days modulated ±30% by growth rate and snapped to the existing `WATER_OPTIONS` presets, with ties breaking toward the **longer** interval because overwatering is cumulative and kills while underwatering self-corrects. Fertilise and repot values are parsed from prose ("monthly in spring and summer", "Every 1–2 years"), and the 20 genera with no stated fertiliser frequency are filled by the same growth-rate rule. Note the engine still has one flat interval per kind, so a plant is fed year-round even though the source scopes feeding to spring and summer. (2) **The attribution is a licence obligation, not decoration** — CC BY 4.0 requires a visible credit and a backlink, which is why `SettingsScreen/CreditsCard` exists; do not remove it or the PlantSolve line, and keep each genus row's PlantSolve profile URL in `source_url`. `resolveCare` resolves alias → genus → family → `FALLBACK_CARE`, and logs `No care data for identified plant` on the `CareSource.None` branch only — that log is how real coverage gets measured, and coverage is currently **unmeasured**, so the 76-genus figure is not a hit rate. **`resolveCare` stays pure and synchronous, but takes the loaded `CareReference` as its first argument** — `resolveCare(reference, lookup)` — so it remains unit-testable with a small fixture and never touches the DB. The data access sits behind `src/services/server/care-reference/index.ts`, which carries `import 'server-only'` and is awaited **once per identification**, not once per result; the loader lowercases the family keys when building the map, so `resolveCare` does a plain lookup and no case-insensitive scan. There is **one** alias map now, and the full-name keys are what keep it safe: `Dracaena trifasciata` is an alias row keyed by the full scientific name (redirecting to `sansevieria`) while `Dracaena fragrans` resolves through the `dracaena` genus row, so collapsing species entries into bare-genus keys would silently corrupt corn plants. The old alias guard test went with the bundled table; the unique index on (`match_type`, `match_key`) is what keeps each alias a single row, and the species-then-genus lookup order is what keeps the two meanings apart. The `AddPlantForm` hint is correspondingly three-state (genus / family / none) and must not be collapsed back to one string: the previous "based on the identified species" copy was an unsourced claim the app could not back.

- **Functions run in `fra1` (Frankfurt), pinned in `vercel.json` — not Vercel's Hobby default `iad1`** (2026-09-20). Vercel places every new project's functions in Washington DC (`iad1`), which puts a ~90 ms-each-way detour in front of *every* request for a user in Europe — a ~200 ms floor that no amount of query tuning can remove. The tell was in the response header: `x-vercel-id: fra1::iad1::…`, i.e. the Frankfurt edge handing off to a US function, with a warm `/login` (which touches no database at all) measuring 200–550 ms TTFB from Amsterdam. `vercel.json`'s `regions: ["fra1"]` puts function execution in the same city as the Neon database, which cuts that floor to **62–68 ms** warm (measured from Amsterdam on a reused connection, down from 200–550 ms); verify with `curl -sI https://sprout.radualex.me/login | grep x-vercel-id` and expect `fra1::fra1`, or read `regions` off the deployment object. **Hobby allows exactly one function region** (Pro allows 5), so there is no multi-region failover — but there never was, so nothing is traded away here. The file is the source of truth rather than the dashboard setting because `vercel.json` travels through CI's `vercel pull` → `vercel build --prod` → `deploy --prebuilt` path and cannot drift from the commit. **`Settings → Functions → Function Regions` is a different thing and must not be read as the effective region**: it is the *project default*, it does not update to reflect a `vercel.json` override, and it still read `iad1` while every deployment ran in `fra1`; it was set to `fra1` too on 2026-09-20 so the two agree, but the deployment's `regions` field — and the deployment's Resources tab — is what reports where functions actually run. **Do not** delete `vercel.json` as redundant, and **do not** move functions back to `iad1`: a US region only pays off if the database moves with it, and Neon is already in Frankfurt — splitting them re-creates the same ~90 ms hop on *every* database round trip, of which an authed page makes at least two (`requireUser()`'s session lookup, then the plants query). **Revisit if** the user base stops being Europe-only: on Hobby that means picking the single region that penalises fewer people.

- **`@neondatabase/serverless`: investigated and NOT adopted** (2026-09-20). Production already runs on Neon — `DIRECT_URL` is the unpooled connection `drizzle.config.ts` migrates through, while the app uses the pooled `DATABASE_URL` — so this was never a migration, it is a driver swap, and the answer is no. The driver replaces TCP with HTTP/WebSockets, which **plain Postgres does not speak**: it cannot connect to the `postgres:16-alpine` container that `docker-compose.yml` and `.env.example` point at, and Neon's own docs say using it locally "requires a local instance of Neon's proxy" — a new container for the dev environment, to replace one that already works. Nothing runs on Edge (there is no `export const runtime = 'edge'` anywhere in `src/`), which is the only thing the driver exists for; Node.js + Fluid compute + the existing `pg` pool is the correct shape here, and the latency it looks like it would fix was already fixed by the `fra1` region move. The cost is not just the import: the `bytea` custom type in `src/lib/db/schema.ts` leans on node-postgres mapping `bytea ↔ Buffer` for free, and that type flows through `types.ts` → `queries.ts` → `readPlantPhoto` → the photo route, so the swap puts every plant photo behind a rework. **Do not** re-add this to the roadmap as a pending optimisation. **Revisit only if** the app moves to the Edge runtime — which is itself the larger decision, and nothing in the app currently argues for it.

- **Web Push over a hosted platform, and the periodic-sync path that never worked** (2026-09-20). Reminders are the product, so the app-closed case had to be solved properly rather than approximated. **`web-push` + a Vercel Cron was chosen over OneSignal** because OneSignal requires its own service worker for delivery (its iOS requirements say so explicitly) and `sw.js` here is hand-written around hard-won rules — the `?v=` BUILD_ID cache invalidation, the RSC-payload guard, never caching documents — so merging a third-party worker into it is the expensive part; `web-push` needed one added listener instead, and keeps delivery out of a third party's hands. **The cron is daily, and the de-duplication has to be day-granular for that to work.** Vercel Hobby permits cron jobs at most once per day with ±59 min precision (anything more frequent fails the deploy) and the due dates are day-granular, so one run per day is exactly the product requirement — but the check first shipped as a **rolling 24 h window** (`now - lastNotified >= DAY_MS`), which is not the same thing, and it broke the app-closed case in two ways. (a) Hobby's ±59 min jitter means a run landing less than 24 h after the previous one filters *every* task, silently skipping that day. (b) Because the in-app watcher writes the same `lastNotified`, any app open within 24 h before the cron cancelled that day's push — so an active user could never receive one. Fixed 2026-09-22 by `isNotifiedToday` in `src/helpers/care`, which compares **UTC calendar days** (`Math.floor(timestamp / DAY_MS)`) and is now the single shared check in both the cron route and the watcher. UTC is deliberate: the server has no `TZ`, so day arithmetic needs a fixed zone, and an 08:00 UTC cron means a notification from the previous UTC day never blocks today's run. **Do not reintroduce a `now - last >= DAY_MS` comparison** — a rolling delta is not day granularity, and this note previously (and wrongly) asserted the two were equivalent. The schedule is `0 8 * * *` in `vercel.json` — 09:00–09:59 CET in winter and 10:00–10:59 CEST in summer, picked so the reminder never lands before 9 AM local: a fixed UTC cron cannot hold a local wall-clock hour across DST, and Hobby's ±59 min jitter widens the window on top of that. **The `periodicsync` path was deleted, and it is worth knowing why it never worked**: the handler forwarded a `care-check` message to `clients.matchAll()`, and with the app closed there are no clients, so the message went nowhere — the worker never fetched plants or called `showNotification` itself. It was a no-op in precisely the case it existed for, on the only platform that implements it. The in-app watcher (mount, focus, hourly) stays: it serves the open-app case, and sharing `isNotifiedToday` with the cron is what keeps the two from double-notifying — an open-app reminder and the cron's push are at most one per task per UTC day. The trade-off is deliberate and worth knowing: if the user opens the app first, they have already been reminded and the cron will not push again that day. **Safari requires the notification to be shown immediately** — Apple's docs state that an invisible push handler gets the permission revoked — so the `push` listener must never defer `showNotification`; and that route must never move to the Edge runtime, because `web-push` needs Node `crypto`. `setVapidDetails` validates its arguments, so it is called inside the handler and never at module scope, which would break `next build` the way `BETTER_AUTH_URL` did. **Needs four env vars in the deployment** — `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `CRON_SECRET` — and is inert without them: the app still runs, and reminders fall back to the in-app watcher. **`NEXT_PUBLIC_VAPID_PUBLIC_KEY` must stay Config, not Sensitive** — `vercel pull` redacts every Sensitive var to the literal `[SENSITIVE]`, and because a `NEXT_PUBLIC_` value is inlined at build time the client bundle would ship that string as the VAPID key: `decodeVapidPublicKey` throws, `subscribeToPush`'s rejection is swallowed, and push never subscribes with nothing in the logs. Verified 2026-09-20 by pulling the production environment — the Config var returns its real value while the other ten come back as placeholders. That is also why `deploy.yml` needs no change for this feature: the three vars it overrides are build-time reads *and* Sensitive, while the public key is neither.

- **Three further push fixes, all aimed at failures that were previously silent** (2026-09-22). (1) **`recordNotified` now runs only when at least one `sendNotification` actually succeeded.** It used to run unconditionally after the per-subscription loop, so a transient non-410 failure was logged, swallowed, and still marked the task notified — eating that day's reminder. (2) **The subscription is re-established on every app load** by `ensurePushSubscription` (`src/services/notifications`), which re-POSTs an existing `pushManager.getSubscription()` or subscribes when there is none, and `sw.js` gained a `pushsubscriptionchange` handler for Chrome/Android endpoint rotation. Before this, `subscribeToPush` was reachable *only* from the permission toggle: once permission was granted the Enable button disappeared, so an endpoint that rotated — or one created outside standalone mode on iOS — 410'd, had its row deleted by the cron's prune, and stayed permanently dead with nothing in the logs. The worker handler must skip when `oldSubscription` is absent, because the worker cannot read the VAPID public key and subscribing without it creates a keyless subscription that `sendNotification` rejects. (3) **The cron warns instead of failing silently**: `console.warn` when `users === 0` (no subscriptions at all — the condition that made the original bug invisible) or when it pruned endpoints. A routine summary is deliberately not logged, because `no-console` allows only `error`/`warn` and a daily success line as a warning is noise. **The response body is the durable per-run record** — `curl -s https://sprout.radualex.me/api/cron/care-check -H "Authorization: Bearer $CRON_SECRET"` returns `{users, notifications, removed}`, and Vercel runtime logs retain only ~1 h on Hobby, so a log line would not have survived to diagnose this anyway.

- **"Send a test notification" now sends a real push** (2026-09-22). It used to call `registration.showNotification(...)`, a purely local notification, so "Test notification sent." was true even when the device had no push subscription — which is precisely what made a dead pipeline look healthy for two days. It now POSTs to `/api/push/test`, which resolves the caller's subscriptions through `sendPushToUser` and returns `{sent, removed}`. **`sent: 0` is split across two statuses, because it has two causes and conflating them is its own lie**: the route first counts the caller's subscriptions, so **no subscription at all yields 409** ("No push subscription is registered for this device") while **subscriptions that exist but were all rejected yields 502** ("The push service rejected the message"). It originally answered 409 for both — which, caught by driving the real UI in a browser, told the user their device had no subscription when in fact FCM had refused the send. Note `sent > 0` means the push service *accepted* the message, not that the device displayed it — the notification arriving is the proof. The payload is deliberately untagged (`PUSH_TEST_PAYLOAD`), because a tag would make a second tap silently replace the previous notification without re-alerting, which reads as "nothing happened". The send loop itself now lives in `sendPushToSubscriptions` (`src/services/server/push`), shared by the cron route and the test route so the two cannot drift, and `setVapidDetails` moved with it — which also keeps it out of module scope, where it would break `next build`.

- **Web Push is free and does not transit Vercel — there is no per-notification quota to exhaust** (2026-09-22). `web-push` posts each message **directly to the browser vendor's push service** — the `endpoint` stored in `push_subscriptions.endpoint` (`fcm.googleapis.com` for Chrome/Android, Apple's push service for Safari/iOS, Mozilla's for Firefox) — so Vercel never sees a push and cannot meter one. What Vercel meters is **function invocations**: the test button costs one per tap, the same as any page load, and the cron costs one per day. So `POST /api/push/test` has no quota to abuse, and it is `requireUser()`-gated and self-scoped — it resolves only the caller's own subscriptions, so a user can only ever push at their own devices, and a caller with no subscription gets **409 without sending**. The realistic harm is therefore self-inflicted spam, not cost. Bounded 2026-09-22 with a **client-side cooldown only** (`TEST_PUSH_COOLDOWN_SECONDS` = 15 in `SettingsScreen/constants.ts`): the button disables and counts down while a test is in flight and for 15s after, which covers the actual failure mode (impatient tapping) with no migration. **Rejected alternatives, recorded so they are not re-evaluated.** (1) **A DB-backed per-user cap** — precise and unbypassable, but it needs a migration and a new table to defend against a threat that is currently self-inflicted only; add it if strangers start signing up. (2) **A Vercel WAF rate-limit rule** — available on Hobby (1 free rule per project, up to 1M allowed requests/month, fixed window, 10s–10min, keyed on IP/JA4 digest; arbitrary keys such as user ID are Enterprise, and Hobby allows up to 3 custom firewall rules in total) and zero code, but rejected because it lives in the dashboard/API rather than the repo and so drifts from the commit — the same reasoning that made `vercel.json` the source of truth for the `fra1` region. **Do not** describe push as metered or quota-limited anywhere in user-facing copy.

- **Test fixtures use one module-scope `props` object — and two constraints shape what can go in it** (2026-09-22). The `frontend-code-conventions` skill mandates `const props: React.ComponentProps<typeof X> = { … }` at module scope, spread into every `render`, and overridden per test by spreading the defaults first. The repo had drifted badly — only 2 of 15 component test files followed it — so all 15 were converted in one sweep. Two things are worth knowing before writing one. (1) **`children` must not live in the fixture.** `@eslint-react/jsx-no-children-prop` resolves the spread and rejects it, so children are always passed as JSX children (`<Button {...props}>Save</Button>`) rather than as a prop — which is why `Button`'s fixture is legitimately empty, and why an "icon-only" test must not reach for `children={undefined}` either. (2) **A discriminated-union component needs a branch-typed fixture for the branch switch.** `Button`'s `Props` is `ButtonProps | AnchorProps`, discriminated by `href` (`href?: never` vs `href: string`), so spreading a union-typed fixture and *then* adding `href` cannot type-check — the anchor test spreads a second `anchorProps: AnchorProps` object instead. That is a real exception to "one props object", not laziness: the union makes a single fixture unable to satisfy both branches.

- **Test fixtures are inline, and there are no loops in tests** (2026-09-22). Four rules, all in the `frontend-code-conventions` skill's Testing section, applied across every suite. (1) **No fixture helper functions in a test file.** A local `const makeTask = (daysUntil) => ({ … })` hides what the test feeds the component and its parameters become a second, weaker type that drifts — the same failure mode as the banned `renderX()`. Each fixture is written inline at the point of use; eight helpers (`makeTask`, `makeResult`, `pageView`, `makeSubscription`, `pushError`, `lastCareAt`, `imageForm`, `emptyForm`) were inlined across 46 call sites. (2) **No fixture constants either.** A module-scope `const VALUE = '2026-09-12'` has the same problem one step removed; there is no size or reference-count threshold, so `VALID_ID` was inlined 26+ times and a data table went into its `it.each` call. (3) **No loops in a test body.** A `for…of` inside an `it` collapses several cases into one pass/fail; unroll into explicit assertions, or use `it.each` when the cases really are an input/expected table — `it.each` is sanctioned because it generates separate named tests. (4) **A binding used once is noise.** `const watered = screen.getByLabelText('Watered');` followed by `expect(watered)…` says exactly what `expect(screen.getByLabelText('Watered'))…` says, with one more name for the reader to hold — hoist into a `const` only when the value is genuinely reused. **Three carve-outs, all forced rather than stylistic.** A `className` marker such as `CALLER_CLASS_NAME` must stay a constant: `sprout/no-literal-classname` rejects a string literal in a `className` attribute, so inlining it is a build break, not a style choice. And harness/oracle helpers stay — environment fakes (`createStream`, `stubFetch`, `stubMatchMedia`, `overrideNavigatorProperty`, `mockResponse`), event adapters that exist because inline handlers are banned (`handleFormSubmit`), and independent oracles used so an assertion does not mirror the implementation (`startOfLocalDay`, `utcDate`). The test is whether the helper hides the fixture's *shape*: if the test's own data is already explicit in the call's arguments, it is harness. `test/vitest/data/` holds the shared fixtures (`plant.mock.ts` with its `makePlant` builder, and `care-reference.mock.ts`); do not add more beyond those without a reason of the same size — `care-reference` earned its file by being a ~40-line object repeated at nine call sites, where inlining doubled the suite from 346 to 742 lines.

- **The `Mocks` import group only works because the catch-all group now excludes it** (2026-09-22). The `import-grouping` rule allows an import in a group when *any* group whose `matches` hit it carries that label — so an import is legal in the **unlabeled** group whenever the first (unlabeled) catch-all group matches it. That first group was `/^(?:\w|@\w).*$/`, which matches every aliased specifier including `@test/vitest/data/plant.mock`; the `Mocks` group could therefore only ever *permit* a label, and nothing forced mocks into it — `// Mocks` was dead config that no import used. Fixed by adding a negative lookahead to the catch-all: `/^(?!.*\.mock$)(?:\w|@\w).*$/`. A mock import left unlabeled now fails with `Importing from '@test/vitest/data/plant.mock' is not allowed in unlabeled group`, which is what makes the group real; all nine mock imports across eight test files moved under `// Mocks`. **Ordering matters**: `Mocks` sits between `Hooks` and `Services` in the groups array, so in a file it belongs after `// Helpers`/`// Hooks` and before `// Services`/`// Database`/`// Types`. **Do not remove the lookahead** — without it the group silently reverts to dead config, and do not "simplify" it back to the bare catch-all.

- **The push send loop is concurrent, and a failed prune is contained** (2026-09-22). `sendPushToSubscriptions` (`src/services/server/push`) sends via `Promise.all` over `.map` instead of a sequential `for…of`, then tallies with a *synchronous* `reduce` over a named `PushOutcome` enum (`types.ts`). Two deliberate choices, recorded so they are not "restored": (1) **concurrency** — a user's devices are pushed concurrently; nothing depended on order (each 410 deletes a distinct row by unique `endpoint`, `setVapidDetails` is still called once beforehand, and no caller reads intra-batch order). (2) **a failed 410 prune is contained** — `pruneSubscription` logs and returns `Failed` rather than letting a DB error reject the batch, so one bad delete cannot cost the other devices' results. The public contract `(subscriptions, payload) => Promise<PushSendResult>` is unchanged, which is what keeps the cron's `result.sent > 0` gate — the thing deciding whether a plant is marked notified — intact, and left the cron route and `/api/push/test` with zero edits. An async `reduce` with a `Promise` accumulator was evaluated and rejected: it stays sequential anyway while allocating a promise and a fresh accumulator object per step. Note there is **no** convention mandating `reduce` — the only other `.reduce` in `src` is a synchronous array build in `helpers/care`.

## Tools

Reference for the toolchain. Each entry: what it is, why we chose it, and the gotchas that matter.

### Runtime & package management

| Tool         | Version | Used for                               | Notes                                                                                                          |
| ------------ | ------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Bun          | 1.3.x   | Installer, script runner, JS runtime   | `bun install`, `bun run <script>`, `bunx <cli>`. Reads `.env.local` automatically. Never use npm/yarn. |
| Node         | 24.x    | Underlying runtime (Next runs on it)   | Only relevant when debugging Next's server directly.                                                           |
| Next.js      | 16.3.x  | App Router framework (Turbopack build) | Middleware was renamed to**proxy**; `proxy.ts` lives in `src/`. `next build` does not run ESLint.  |
| React        | 19.2.x  | UI library                             | Server components by default; client components opt in with`'use client'`.                                   |
| TypeScript   | 6.x     | Types                                  | `tsc` runs via `bun run build` (after `build:rules`); `tsconfig.json` has `@/*` → `./src/*`.      |
| classnames   | 2.5.x   | Conditional class composition          | `classNames(styles.root, { [styles.x]: cond })` — never inline in JSX; see conventions.                     |
| lucide-react | 1.31.x  | Icons                                  | Tree-shakeable, server-compatible.`CARE_META` and `TABS` hold `LucideIcon` refs, not emoji strings.      |
| lodash-es    | —      | Utilities over hand-rolled helpers     | E.g.`capitalize`; see conventions.                                                                           |

### Database

| Tool                   | Version                     | Used for                 | Notes                                                                                        |
| ---------------------- | --------------------------- | ------------------------ | -------------------------------------------------------------------------------------------- |
| Postgres               | 16 (`postgres:16-alpine`) | Data store               | Runs in Docker (`db` compose service).                                                     |
| Drizzle ORM            | 0.45.x                      | Query builder + types    | Schema in`src/lib/db/schema.ts`; queries in `src/lib/db/queries.ts`.                     |
| drizzle-kit            | 0.31.x                      | Migrations + studio      | `db:generate`, `db:migrate`, `db:studio`.                                              |
| `pg` (node-postgres) | 8.x                         | Postgres driver          | Maps`bytea` ↔ `Buffer` natively. Prod swaps to `@neondatabase/serverless`. |
| Neon                   | (planned)                   | Serverless prod Postgres | Not used yet — see roadmap.                                                                 |

Gotchas: Drizzle's pg driver has **no binary column type** — `schema.ts` defines a custom `bytea` type via `customType`. The database is `sprout` (user/pass `sprout/sprout` locally).

### Auth

| Tool                | Version | Used for              | Notes                                                                                                |
| ------------------- | ------- | --------------------- | ---------------------------------------------------------------------------------------------------- |
| Better Auth         | 1.6.x   | Auth server + client  | `src/lib/auth/index.ts`; Drizzle adapter.                                                          |
| @better-auth/cli    | 1.4.x   | Generates auth schema | `bunx @better-auth/cli generate` → `src/lib/db/auth-schema.ts` (lint-ignored, don't hand-edit). |
| better-auth/react   | —      | Client hooks          | `authClient` in `src/lib/auth/auth-client.ts` (signIn/signUp/signOut).                           |
| better-auth/cookies | —      | Proxy cookie check    | `getSessionCookie` for the optimistic `src/proxy.ts` check.                                      |

Security model: three layers — `proxy.ts` (cookie-only, no DB), `requireUser()` in every server component/action/route, and per-user `userId` scoping on every query.

### Linting & formatting

| Tool                     | Version                         | Used for                                                                                                                                                 | Notes                                                                                                                                                                                                                  |
| ------------------------ | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ESLint                   | 10.8.x                          | JS/TS/React linting                                                                                                                                      | Flat config; type-aware and slow.                                                                                                                                                                                      |
| @onefinity/eslint-config | 4.x                             | Base flat config + custom`import-grouping` rule                                                                                                        | Groups imports by kind (Constants/Components/Helpers/Hooks/Services/Lib/Styles/Types + Schema).                                                                                                                        |
| eslint-plugin-react      | 7.x                             | React rules                                                                                                                                              | `function-component-definition` requires arrow functions everywhere.                                                                                                                                                 |
| Custom rules             | —                              | `eslint-rules/` (`jsx-props-inline`, `use-component-props-string`, `no-literal-classname`, `no-inline-object-literal`, `no-inline-handlers`) | Written in TS, compiled to JS by`build:rules` (`tsc -p tsconfig.eslint-rules.json`). Lint-ignored themselves. Enforce the handle*/useCallback, styles-module, multiline-object, and no-inline-handler conventions. |
| eslint-config-next       | 16.3.x                          | Reference only                                                                                                                                           | Installed but the project relies on the custom onefinity config; not wired in.                                                                                                                                         |
| Stylelint                | via @onefinity/stylelint-config + @dreamsicle.io/stylelint-config-tailwindcss | CSS linting (incl. Tailwind v4 directives)                                                                          | Run with`bun run lint:css` / `lint:fix`. A `**/*.module.css` override keeps camelCase `selector-class-pattern`.                                                                                                    |
| eslint-plugin-better-tailwindcss | 4.7.x                  | Tailwind class order + canonical classes in `@apply`                                                                                                    | Enabled on `**/*.css` through `@eslint/css` + `tailwind-csstree`; `entryPoint` is `src/app/globals.css`. `lint:fix` sorts `@apply`.                                                                                |

Gotcha: `eslint --fix` will happily mangle files — prefer targeted edits, then run `bun run lint` once.

### Styling (Tailwind v4 + CSS Modules)

| Tool                  | Used for                       | Notes                                                                                                                                                                                                                                                                             |
| --------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tailwind CSS          | 4.3.x — utilities + tokens     | Entry `src/app/globals.css` via `@tailwindcss/postcss`. Imports only `tailwindcss/theme.css` (layer `theme`) + `tailwindcss/utilities.css` (layer `utilities`) — **preflight is deliberately omitted**. Tokens are declared with `@theme static` so they emit as live `:root` vars. |
| `@utility ui-*` atoms | Shared, repeated patterns      | `src/styles/shared/ui.css`, bare-imported (no `layer()`) by `globals.css`; component modules `@apply` them. **Never put class-descendant selectors in a shared `@utility`** — CSS-module hashing makes them silently stop matching.                                                 |
| CSS Modules           | Component-scoped styles        | Co-located `styles.module.css` with `@reference "<relative>/app/globals.css";` at the top and `@apply` for every rule. No inline utility strings in JSX (enforced by `sprout/no-literal-classname`).                                                                               |

SCSS is fully retired: no `sass`, no `.module.scss`, no `src/styles/shared/*` partials and no `globals.scss`. Owned design-system blocks live in `src/design-system/` (`Button`, `Select`, `AlertDialog`); `@base-ui/react` is the headless primitive layer those blocks wrap — never import a Base UI primitive directly in feature code.

Use relative units — prefer `rem`, so the UI stays responsive when the viewport is resized. `px` is the exception, for what genuinely must not scale (e.g. `1px` hairline borders); never hardcode `px` for spacing, type, widths or radii, and avoid arbitrary `px` utilities like `p-[16px]` (use the Tailwind scale or a `rem` value). React numeric style values are pixels — use relative strings (`{ gap: '0.75rem' }`) and pass relative strings to `lucide-react`'s `size` prop (`size="1rem"`); only `0` stays a bare number.

### Docker

| Tool              | Used for                | Notes                                                                                                                          |
| ----------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Docker Compose v2 | Local dev orchestration | `db` (Postgres) + `app` (Next dev) services; `app-prod` (profile `prod`) serves the production image on :3001.         |
| Dockerfile        | Multi-stage build       | `dev` target (bind-mounted source, HMR) and `prod` target (standalone output on `oven/bun:1-alpine`, `bun server.js`). |

Gotchas: inside compose the DB hostname is `db`, not `localhost` (`DATABASE_URL=postgres://sprout:sprout@db:5432/sprout`). macOS volume-mount HMR is slower than native dev. Prod parity locally: `docker compose --profile prod up --build app-prod` (serves :3001).

### PWA

| Tool             | Used for                | Notes                                                                                                                                                                                    |
| ---------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Service worker   | `public/sw.js`        | Cache`sprout-v4`: cache-first for a small static allowlist (icons, manifest) only; **never** caches documents, `/api`, RSC payloads or `/_next/static`. Dev unregisters any existing worker and purges caches (`SiteCore`). Handles notification clicks (navigating to the payload's `url`) and the `push` listener that displays pushed reminders. |
| Web App Manifest | `src/app/manifest.ts` | Generated route (`MetadataRoute.Manifest`) replacing the static `manifest.webmanifest`.                                                                                              |
| Notification API | —                      | Permission,`showNotification`. In-app checks run on mount, on focus and hourly while open; Web Push covers the app-closed case. No periodic background sync — it cannot work with the app closed (see the Notes entry).                                                                                        |

### External services

| Service      | Used for             | Notes                                                                                                                        |
| ------------ | -------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| PlantNet API | Plant identification | Proxied server-side by`src/app/api/identify/route.ts`; key from `PLANTNET_API_KEY` env.     |
| Google OAuth | Sign-in              | Better Auth social provider; needs`GOOGLE_CLIENT_ID/SECRET` + redirect `http://localhost:3000/api/auth/callback/google`. |

### Scripts (`package.json`)

| Script                                           | Command                                    | Purpose                                      |
| ------------------------------------------------ | ------------------------------------------ | -------------------------------------------- |
| `start`                                        | `next dev`                               | Dev server (Turbopack).                      |
| `build`                                        | `build:rules && next build`          | Compile ESLint rules, then production build. |
| `start:prod`                                   | `next start`                             | Serve the production build.                  |
| `lint` / `lint:fix`                          | `lint:js ; lint:css`                    | Run both linters (JS/TS then CSS).           |
| `lint:js`                                      | `eslint "src/**/*.{ts,tsx}" …`          | ESLint on TS/TSX only.                       |
| `lint:css`                                     | `stylelint "src/**/*.css" ; eslint …`   | Stylelint + Tailwind class order on CSS.     |
| `db:up` / `db:down`                          | `docker compose up -d db` / `down`     | Start/stop the dev Postgres.                 |
| `docker:build:prod`                            | `docker build --target prod -t sprout .` | Build the standalone production image.       |
| `db:migrate` / `db:generate` / `db:studio` | `drizzle-kit …`                         | Apply / create migrations; inspect data.     |
| `build:rules`                                  | `tsc -p tsconfig.eslint-rules.json`      | Compile custom ESLint rules.                 |
